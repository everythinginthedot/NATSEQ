# Benchmark

This is where NATSEQ's accuracy is being validated — against simulated data with a known ground truth, and against real public datasets (SG-NEx) checked by manual curation.

**This page is a placeholder.** The benchmark itself is real and ongoing, but the write-up — methodology, datasets used, current results — isn't ready yet. It'll be filled in soon.
